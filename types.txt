U uint
I int

N uints
B bytes
S string -> uints (vusv)

L list -> tuple<type, tuple<T, T, ..>>
P tuple

A alias -> tuple<typeid, val>
E enum -> tuple<typeid, varid>
C choice -> tuple<typeid, varid, val>
R struct -> tuple<typeid, tuple>

T type -> tuple<uint (tag), val (or unit)>
D typeid -> tuple<uint (typeid-type), val>


unit -> tuple<>
bool -> enum

option -> enum

map -> list<tuple>
