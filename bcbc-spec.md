定义**基本上限**为**128位无符号整数能够表示的长度上限**。
<!-- 在所有用到上限的地方添加用于编解码器的上限限制点 -->

# 基本编码方式

### 无符号整数 (`uint`)

一个最大数值长度128位的无符号整数。

#### 编码结构

该整数的ULEB128编码。

### 有符号整数 (`int`)

一个最大数值长度128位的有符号整数。

#### 编码结构

该整数的SLEB128编码。

### 无符号整数串 (`uints`)

一个长度为基本上限（可为0）的128位无符号整数串。

#### 编码结构

长度的ULEB128编码 + 第一个无符号整数的ULEB128编码 + ... + 最后一个无符号整数的ULEB128编码。

### 字节串 (`bytes`)

一个长度为基本上限（可为0）的字节串。

#### 编码结构

长度的ULEB128编码 + 第一个字节 + ... + 最后一个字节。

### 元组（值串） (`tuple`)

一个长度为基本上限（可为0）的值串。

#### 编码结构

长度的ULEB128编码 + 第一个值 + ... + 最后一个值。

# 类型

以下所有的编码结构均为1字节tag的ASCII码 + 所用编码方式。

## 基本类型

### 无符号整数类型 (`uint` `tag='U'`)

一个最大数值长度128位的无符号整数。

#### 编码结构

使用*无符号整数编码*。

### 有符号整数类型 (`int` `tag='I'`)

一个最大数值长度128位的有符号整数。

#### 编码结构

使用*有符号整数编码*。

### 布尔类型 (`bool` `tag='F'`)

有真和假两个值。

#### 编码结构

将真视为1，假视为0，然后使用*无符号整数编码*。

### 无符号整数串类型 (`uints` `tag='N'`)

一个长度为基本上限（可为0）的128位无符号整数串。

#### 编码结构

使用*无符号整数串编码*。

### 字节串类型 (`bytes` `tag='B'`)

一个长度为基本上限（可为0）的字节串。

#### 编码结构

使用*字节串编码*。

### 字符串类型 (`string` `tag='S'`)

一个长度为基本上限（可为0）的Unicode标量值串。

#### 编码结构

将每个Unicode标量值视作一个无符号整数，然后使用*无符号整数串编码*。

## 泛型类型

### 元组（值串）类型 (`tuple` `tag='P'`)

一个长度为基本上限（可为0）的值串。

*可视为可具备基本上限多个泛型参数*。
<!-- 但在schema中不以泛型格式表示 -->

#### 编码结构

使用*元组编码*。

### 列表类型 (`list` `tag='L'`)

一个长度为基本上限（可为0）的特定类型的值的列表。

列表中所有值的类型必须相同。

*具备1个泛型参数*，为列表中所有值的类型。

#### 编码结构

使用一个固定具有2个值的*元组编码*：

第1个值是*布尔值*，表示列表是否为空；

若列表为空，第1个值为假，第2个值为该列表中值的类型的*类型值*；

若列表非空，第1个值为真，第2个值为按序包含该列表中所有值的*元组值*。

### 可选类型 (`option` `tag='O'`)

可能含有1个特定类型的值，也可能不含有值。

*具备1个泛型参数*，为可选值含有值的类型。

#### 编码结构

使用一个固定具有2个值的*元组编码*：

第1个值是*布尔值*，表示可选值是否为空；

若可选值为空，第1个值为假，第2个值为该可选值中值的类型的*类型值*；

若可选值非空，第1个值为真，第2个值为该可选值中包含的*值*。

<!-- 选择is_some主要是机器逻辑的顺畅向人的直觉妥协了，空值为真太反直觉了 -->

## 自定义类型

### 别名类型 (`alias` `tag='A'`)

另一个特定类型的别名。
<!-- 禁止创建是自己的别名的类型 -->
<!-- 可以用validater为其值增加限制 -->

*可具备基本上限多个泛型参数*。

#### 编码结构

使用一个固定具有3个值的*元组编码*：

第1个值为该自定义类型的*类型ID值*；

第2个值为按序包含所有泛型参数的类型值的*元组值*；

第3个值为该别名类型值中包含的*值*。

### 枚举类型 (`enum` `tag='E'`)

可能为其中一个有序的特定的枚举值的类型。

最多可以有基本上限多个可能的枚举值。

#### 编码结构

使用一个固定具有2个值的*元组编码*：

第1个值为该自定义类型的*类型ID值*；

第2个值为从0开始的枚举值的编号的*无符号整数值*。

### 可区分联合类型 (`choice` `tag='C'`)

可能为其中一个有序的特定的枚举值，每个枚举值中含有一个指定类型的值的类型。

最多可以有基本上限多个可能的枚举值。

*可具备基本上限多个泛型参数*。

#### 编码结构

使用一个固定具有4个值的*元组编码*：

第1个值为该自定义类型的*类型ID值*；

第2个值为按序包含所有泛型参数的类型值的*元组值*；

第3个值为从0开始的枚举值的编号的*无符号整数值*；

第4个值为该枚举值中包含的*值*。

### 结构体类型 (`struct` `tag='R'`)

有序的特定的多个类型的值，每个值在类型定义中有名字的列表。

最多可以有基本上限多个字段。

*可具备基本上限多个泛型参数*。

#### 编码结构

使用一个固定具有3个值的*元组编码*：

第1个值为该自定义类型的*类型ID值*；

第2个值为按序包含所有泛型参数的类型值的*元组值*；

第3个值为按序包含结构体中所有字段值的*元组值*。

## 特殊类型

### 类型类型 (`type` `tag='T'`)

值表示一个类型（可为未知）的类型。

#### 编码结构

使用一个固定具有2个值的*元组编码*：

第1个值为`type tag`（为tag的小写字母版本，未知为`'0'`）的ASCII码的*无符号整数值*；

第2个值：

如果为未知/基本类型/特殊类型，则为一个*空元组值*，

如果为元组类型，则为按序包含元组中所有值的类型值的*元组值*，

如果为列表类型/可选类型，则为列表或可选值中值的类型的*类型值*，

如果为枚举类型，则为自定义类型的*类型ID值*，

如果为除枚举类型外的自定义类型，则为一个固定具有2个值的*元组值*：

第1个值为自定义类型的*类型ID值*，

第2个值为按序包含所有泛型参数的类型值的*元组值*。

### 类型ID类型 (`type id` `tag='D'`)

值表示一个自定义类型的类型ID，可能为匿名、标准库、第三方。

<!-- 类型ID是否同时作为trait ID？ -->

#### 编码结构

使用一个固定具有2个值的*元组编码*：

第1个值为`type id tag`（匿名为`'x'`、标准库为`'y'`、第三方为`'z'`）的ASCII码的*无符号整数值*；

第2个值：

若为匿名，则为一个空*元组值*，

若为标准库，则为该自定义类型在标准库中的ID的*无符号整数值*，

若为第三方（第三方类型ID的编码还未确定）。

# 类型组合约定

### 单元/单位类型 (`unit`)

表示一个空的占位符。
<!-- 对于构造有效的迭代属性可能有用 -->

用一个空*元组值*表示。

### 键值对类型 (`map`)

特定两个类型的值的键-值对。

用按序包含键和值的*元组值*的*列表值*表示。

<!-- BCBL要使用的无限列表 -->
