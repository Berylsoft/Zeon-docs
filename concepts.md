# 写在前面

其实Zeon的设计目的既不是什么不可变性，也不是什么唯一性透明性通用性，而是一个强迫症（划掉）其实是孤独症者对只存在于自己内心的那个理想世界的建模。（如果不知道的话，我对孤独症者核心特点的理解是「活在自己的世界里」，能够真正理解的只有自己建模出来的虚拟世界。）因此很难说它有什么要满足的「需求」，我只是把脑海中的那个正确的「世界式」描摹出来罢了。至于它能有什么用，还是那句老话，就让时间使之发芽吧。

# 哲学

## 不可变性（可追溯性）

【为什么不可变性很重要？便利的可追溯、回溯和迁移，透明性，唯一性和准确性】

## 唯一性

## 透明性

## 通用性（模块化）

# 基本概念

## 提前说明

以下提及所有**无符号整数**和**有符号整数**理论最大长度均为**128位**（即BCBC中的最大长度值）。

定义**基本上限**为**128位无符号整数能够表示的长度上限**。

## 存储库

每个Zeon服务端实例管理的数据的总集【？】，我们称之为**存储库** (repository / repo)。

## 对象

从「空间」的角度，Zeon的基本单元是**对象** (object)。

每个对象都有其类型，我们称之为**对象类型** (object type)。（Zeon中不同的「类型」概念很多，请读者一定要注意区分。）每个Zeon存储库中，**对象类型ID**(object type id)为无符号整数，随着对象类型在存储库中的增加而自增。

每个对象都有其在对应的对象类型中的ID，我们称之为**对象类型内ID** (object inner id)，为无符号整数，随着对象在指定对象类型中的增加而自增，或者使用范围内随机增加算法（将另外列出）。

**对象类型ID**和**对象类型内ID**连接起来的整体，我们称之为**对象ID** (object id)。

对象自身只是一个对象ID【？】，无法存储任何数据，也无法实现任何功能。对象储存数据是通过向对象附加trait实现的。

每个对象类型需指定至少一个trait作为**必需trait集合** (essential traits set)，属于该对象类型的所有对象都必须附加这些trait。除元数据类型外，必需trait集合在创建类型之后可以增加或者减少，方式是通过具有系统管理员权限的操作者向该对象类型的元数据对象以及该对象类型的所有对象【一定要是系统管理员提交所有对象的修改么？如果要保证在一个提交内完成的话，一个提交是不是可以有多个操作者？是不是可以有author和committer的区别？】提交migration commit。

必需trait集合中包含操作者及相关权限的trait的对象类型被称为**操作者类型** (operator type)，属于该对象类型的对象是**操作者** (operator)。操作者可以对拥有相应权限的对象进行提交（包括创建新的对象）。

每个存储库中都有一些固定的对象类型，其必需trait集合和作用是确定的，用于储存每个存储库的元数据，这些对象类型被称为**元数据类型**。元数据类型中包含该存储库中的对象类型

## trait

**trait**是对象存储数据、实现功能的凭依。每个trait中包含若干属性。
【versioning？】

属性在其对应trait中的ID，我们称之为**trait属性ID** (trait attr id)，为无符号整数，按照trait schema中的顺序依次编号。

**属性类型** (attr type)根据其每次提交的值与该属性的值的关系分为6种：

值在被第一次提交之后即不能被之后的提交改变的属性被称为**常量属性** (const)。

值为最新一次提交的值（即每次提交都会覆盖上次提交的值）的属性被称为**变量属性** (mut)。

以下两个属性类型统称为**迭代属性** (iter)，有按操作者查询值 (query by operator)的功能。

值为每次提交的值的列表的属性被称为**迭代列表属性** (iterlist)。iterlist的值包含一个操作者的全部历史提交，按操作者查询值时取到的是按时间排序的值的列表。

值为每个操作者最新一次提交的值的列表的属性被称为**迭代集合属性** (iterset)。iterset的值仅包含一个操作者的最新提交，按操作者查询值时取到的是一个最新值。

值为指向另外一个对象的对象id，且被指向的对象也有一个该属性类型的属性指向该对象的对象id，这样的属性被称为**成对属性** (pair)。成对属性在提交时，必须在一个提交内同时提交两侧，否则将无法通过检查。

值为每次提交的diff值（diff算法将另外列出）的总和的属性被称为**复杂结构属性** (complex)。复杂结构属性实质上是一种高级的变量属性，它可以被用于值为复杂数据类型的情况，这样就不需要每次修改都保存完整的数据，而只需要保存diff值。

属性可以为**可默认** (defaultable)。可默认属性可以在对象类型元数据里设置默认值；如果设置了默认值，创建对象时可以不提交该属性；进行非提交查询时，查询到的是默认值。

## 提交

从「时间」的角度，Zeon的基本单元是**提交** (commit)。

提交分为**提交头** (commit header)和**提交体** (commit body)两部分。

提交头包含**提交ID** (commit id)、**提交签名** (commit signature)、**提交附加信息**三部分。

以下是提交ID的构成：

**时间戳**，分两部分：

其一为有符号整数，记录自UTC时间2001年1月1日0时整经过的秒数；

其二为无符号整数，记录自上述秒数后经过的微秒数，值在0-1_000_000之间。

**序列码**，为无符号整数，根据实现决定，目的是防止多机器接收提交等情况产生碰撞；

**操作者的对象ID**。

**提交附加信息**中，可以附加如提交来源IP地址等信息。

**提交体**是一个键-值对，以下是键的构成：

**被操作对象的对象ID**；

**被操作trait的ID**；

**被操作属性的trait属性ID**。

**提交ID**和**提交体的一个键**被称为对应被操作属性值的一个**修改指针**。

## 数据类型

Zeon使用**BCBC** (Berylsoft Common Binary Codec)作为**通用的二进制序列化格式**。

### 基本编码方式

**无符号整数** (uint)，为一个最大数值长度128位的无符号整数。编码结构为：该整数的ULEB128编码。

**有符号整数** (int)，为一个最大数值长度128位的有符号整数。编码结构为：该整数的SLEB128编码。

**无符号整数串** (uints)，为一个长度为基本上限（可为0）的128位无符号整数串。编码结构为：长度的ULEB128编码 + 第一个无符号整数的ULEB128编码 + ... + 最后一个无符号整数的ULEB128编码。

**字节串** (bytes)，为一个长度为基本上限（可为0）的字节串。编码结构为：长度的ULEB128编码 + 第一个字节 + ... + 最后一个字节。

**元组（值串）** (tuple)，为一个长度为基本上限（可为0）的值串。编码结构为：长度的ULEB128编码 + 第一个值 + ... + 最后一个值。

以下所有的编码结构均为1字节tag的ASCII码 + 所用编码方式。

### 基本类型

**无符号整数类型** (uint)，tag='U'，为一个最大数值长度128位的无符号整数。编码结构为：使用*无符号整数编码*。

**有符号整数类型** (uint)，tag='I'，为一个最大数值长度128位的有符号整数。编码结构为：值的*有符号整数编码*。

**布尔类型** (bool)，tag='B'，有真和假两个值。编码结构为：将真视为1，假视为0，然后使用*无符号整数编码*。

**无符号整数串类型** (uints)，tag='N'，为一个长度为基本上限（可为0）的128位无符号整数串。编码结构为：使用*无符号整数串编码*。

**字节串类型** (bytes)，tag='B'，为一个长度为基本上限（可为0）的字节串。编码结构为：使用*字节串编码*。

**字符串类型** (string)，tag='S'，为一个长度为基本上限（可为0）的Unicode标量值串。编码结构为：将每个Unicode标量值视作一个无符号整数，然后使用*无符号整数串编码*。

### 泛型类型

**元组（值串）类型** (tuple)，tag='P'，为一个长度为基本上限（可为0）的值串。*可视为具备基本上限多个泛型参数*。【但在schema中不以泛型格式表示】编码结构为：使用*元组编码*。

**列表类型** (list)，tag='L'，为一个长度为基本上限（可为0）的特定类型的值的列表。列表中所有值的类型必须相同。*具备1个泛型参数*，为列表中所有值的类型。编码结构为：使用一个固定具有2个值的*元组编码*。其中第1个值是一个值必须为0或1的*无符号整数值*，表示列表是否为空；若列表为空，第1个值为1，第2个值为该列表中值的类型的*类型值*；若列表非空，第1个值为0，第2个值为按序包含列表中所有值的*元组值*。

**可选类型** (option)，tag='O'，可能含有1个特定类型的值，也可能不含有值。*具备1个泛型参数*，为可选值含有值的类型。编码结构为：使用一个固定具有2个值的*元组编码*。其中第1个值是一个值必须为0或1的*无符号整数值*，表示可选值是否为空；若可选值为空，第1个值为1，第2个值为该可选值中值的类型的*类型值*；若可选值非空，第1个值为0，第2个值为可选值中包含的*值*。

### 自定义类型

**别名类型** (alias)，tag='A'，为另一个特定类型的别名。【禁止创建是自己的别名的类型】【可以用validater为其值增加限制】*可具备基本上限多个泛型参数*。编码结构为：使用一个固定具有3个值的*元组编码*。其中第1个值为该自定义类型的*类型ID值*；第2个值为按序包含所有泛型参数的类型值的*元组值*；第3个值为该别名类型值中包含的*值*。

**枚举类型** (enum)，tag='E'，为可能为其中一个有序的特定的枚举值的类型。最多可以有基本上限多个可能的枚举值。编码结构为：使用一个固定具有2个值的*元组编码*。其中第1个值为该自定义类型的*类型ID值*；第2个值为从0开始的枚举值的编号的*无符号整数值*。

**可区分联合类型** (choice)，tag='C'，为可能为其中一个有序的特定的枚举值，每个枚举值中含有一个指定类型的值的类型。最多可以有基本上限多个可能的枚举值。*可具备基本上限多个泛型参数*。编码结构为：使用一个固定具有4个值的*元组编码*。其中第1个值为该自定义类型的*类型ID值*；第2个值为按序包含所有泛型参数的类型值的*元组值*；第3个值为从0开始的枚举值的编号的*无符号整数值*；第4个值为该枚举值中包含的*值*。

**结构体类型** (struct)，tag='R'，为有序的特定的多个类型的值，每个值在类型定义中有名字的列表。最多可以有基本上限多个字段。*可具备基本上限多个泛型参数*。编码结构为：使用一个固定具有3个值的*元组编码*。其中第1个值为该自定义类型的*类型ID值*；第2个值为按序包含所有泛型参数的类型值的*元组值*；第3个值为按序包含结构体中所有字段值的*元组值*。

### 特殊类型

**类型类型** (type)，tag='T'，值表示一个类型（可为未知）的类型。编码结构为：使用一个固定具有2个值的*元组编码*。其中第1个值为type tag（为tag的小写字母版本，未知为'0'）的ASCII码的*无符号整数值*；第2个值如果为未知/基本类型/特殊类型，则为一个*空元组值*，如果为元组类型，则为按序包含元组中所有值的类型值的*元组值*，如果为列表类型/可选类型，则为列表或可选值中值的类型的*类型值*，如果为自定义类型，则为自定义类型的*类型ID值*【泛型？】。

**类型ID类型** (type id)，tag='D'，值表示一个自定义类型的类型ID。可能为匿名、标准库、第三方。编码结构为：使用一个固定具有2个值的*元组编码*。其中第1个值为type id tag（匿名为'x'、标准库为'y'、第三方为'z'）的ASCII码的*无符号整数值*；第2个值若为匿名，则为一个*空元组值*，若为标准库则为该自定义类型在标准库中的ID的*无符号整数值*。
【类型ID是否同时作为trait ID？】

<!-- **单元/单位类型** (unit)，表示一个空的占位符，对于构造有效的迭代属性可能有用。 -->

【BCBL要使用的无限列表】

<!-- **键值对类型** (map)，为特定两个类型的值的键-值对。长度允许为0，最大为【】对。 -->

【上面的文档也使用阿拉伯数字】
